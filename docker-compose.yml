version: '3.8'

services:
  openkeep:
    build: .
    ports:
      - "80:80"
    volumes:
      - openkeep-data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/openkeep.db
    restart: unless-stopped

volumes:
  openkeep-data:
